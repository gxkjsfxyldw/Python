# 绘制直方图

plt.hist （x,bins=10）用法

主要参数

x ：数据

bins：间隔数，默认为10

```
import matplotlib.pyplot as plt
import numpy as np
 
pop = np.random.randint(0,100,100)   #随机数中使包括1和100,的100个随机数
plt.hist(pop,bins=20)   #间隔20，默认是10
plt.show()
```

