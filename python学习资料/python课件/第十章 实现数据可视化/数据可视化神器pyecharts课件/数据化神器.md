 

目前pyecharts的版本已经更新到了1.x版本，而旧版本也就是0.5.x版本在语法上和1.x版本有很大的不同，由于旧版本不再维护，因此本文就从新版本的使用开始说起。

# 柱状图/条形图



```python
from pyecharts.charts import Bar
from pyecharts import options as opts

cata = ['apple','Huawei','xiaomi','oppo','samsung']
data1 = [123,153,89,107,98]
data2 = [56,77,93,68,45]

bar = (
    Bar()
    .add_xaxis(cata)  #设置x宙的数据
    .add_yaxis('线上渠道',data1)  #线上渠道的数据
    .add_yaxis('线下渠道',data2)  #线下渠道的数据
    .set_global_opts(
                     title_opts=opts.TitleOpts(title='Bar-基本示例',subtitle='我是副标题'),
                     toolbox_opts=opts.ToolboxOpts(),#可以理解为工具栏
                     legend_opts=opts.LegendOpts(is_show=True) #图例要不要展示出来
                     )
      )

#渲染,可视化
bar.render()
```

![1](图\1.gif)



在工具栏中可以实现一键堆叠柱状图，当然也可以展现出数据中的最大值与最小值

```python
from pyecharts.charts import Bar
from pyecharts import options as opts

cata = ['apple','Huawei','xiaomi','oppo','samsung']
data1 = [123,153,89,107,98]
data2 = [56,77,93,68,45]

bar = (
    Bar()
    .add_xaxis(cata)  #设置x宙的数据
    .add_yaxis('线上渠道',data1)  #线上渠道的数据
    .add_yaxis('线下渠道',data2)  #线下渠道的数据
    .set_series_opts(
                    label_opts=opts.LabelOpts(is_show=False),
                    markpoint_opts=opts.MarkPointOpts(
                        data=[
                            opts.MarkPointItem(type_='max',name='最大值'),
                            opts.MarkPointItem(type_='min',name='最小值'),
                            opts.MarkPointItem(type_='average',name='平均值')
                              ]
                                                     ),
                    markline_opts=opts.MarkLineOpts(
                        data=[
                            opts.MarkLineItem(type_='min',name='最小值'),#标记线与标记点最大最小值，以及平均值
                            opts.MarkLineItem(type_='max',name='最大值')
                        ]
                    )
                    )
    .set_global_opts(
                     title_opts=opts.TitleOpts(title='最大最小值的展现',subtitle='我是副标题'),
                     toolbox_opts=opts.ToolboxOpts() #可以理解为工具栏
                     )
      )

#渲染,可视化
bar.render()
```

![2](图\2.gif)

设置窗口滑块，拖动查看柱状图

```python
from pyecharts.charts import Bar
from pyecharts import options as opts

cata = ['apple','Huawei','xiaomi','oppo','samsung']
data1 = [123,153,89,107,98]
data2 = [56,77,93,68,45]

bar = (
    Bar()
    .add_xaxis(cata)  #设置x宙的数据
    .add_yaxis('线上渠道',data1)  #线上渠道的数据
    .add_yaxis('线下渠道',data2)  #线下渠道的数据
    .set_global_opts(title_opts=opts.TitleOpts(title='Bar-基本示例',subtitle='我是副标题'),
                     toolbox_opts=opts.ToolboxOpts(),#可以理解为工具栏
                     legend_opts=opts.LegendOpts(is_show=True), #图例要不要展示出来
                     datazoom_opts=opts.DataZoomOpts()  #设置窗口滑块
                     )
      )
#总渲染
bar.render()
```

![3](图\3.gif)

# 折线图

 

其实我们可以在柱状图的工具栏中，点击“切换为折线”将柱状图转换为折线图，当然，我们也可以将折线图还原成柱状图，点击“切换为柱状图”

![4](图\4.gif)

# 饼状图

```python
from pyecharts.charts import Pie
from pyecharts import options as opts

cata = ['apple','Huawei','xiaomi','oppo','samsung']
data1 = [123,153,89,107,98]

pie = (
    Pie()
    .add('',[list(z) for z in zip(cata,data1)],radius='55%') #radius表示圆的半径
    .set_global_opts(title_opts=opts.TitleOpts(title='Pie-基本示例',subtitle='我是副标题'))
    .set_series_opts(label_opts=opts.LabelOpts(formatter='{b}:{d}%'))#数据展现的格式
      )


#总渲染
pie.render()

```

![5](图\5.gif)

当然我们可以调整成为内外圆环

```python
from pyecharts.charts import Pie
from pyecharts import options as opts

cata = ['apple','Huawei','xiaomi','oppo','samsung']
data1 = [123,153,89,107,98]

pie = (
    Pie()
    .add('',[list(z) for z in zip(cata,data1)],
         radius=['40%','65%']) #调整内圆环的半径与外圆环的半径
    .set_global_opts(title_opts=opts.TitleOpts(title='Pie-基本示例',subtitle='我是副标题'))
    .set_series_opts(label_opts=opts.LabelOpts(formatter='{b}:{d}%'))#数据展现的格式
      )


#总渲染
pie.render('内空饼图.html') #保存当前路径名为'内空饼图.html'
```

![6](图\6.gif)

饼图-玫瑰图

```python
from pyecharts.charts import Pie
from pyecharts import options as opts

cata = ['apple','Huawei','xiaomi','oppo','samsung']
data1 = [123,153,89,107,98]

pie = (
    Pie()
    .add('',[list(z) for z in zip(cata,data1)],
         radius=['30%','75%'], #调整内圆环的半径与外圆环的半径
         rosetype='radius'
         )
    .set_global_opts(title_opts=opts.TitleOpts(title='Pie-基本示例',subtitle='我是副标题'))
    .set_series_opts(label_opts=opts.LabelOpts(formatter='{b}:{d}%'))#数据展现的格式
      )


#总渲染
pie.render('m玫瑰饼图.html')
```

![7](图\7.gif)

# 漏斗图

```python
from pyecharts.charts import Funnel
from pyecharts import options as opts

cata = ['访问','收藏','加入购物车','提交单位支付','付款成功']
data1 = [30398,15230,10045,8109,5700]

pie = (
    Funnel()
    .add('用户数',[list(z) for z in zip(cata,data1)],
         sort_='ascending',
         label_opts=opts.LabelOpts(position='inside')
         )
    .set_global_opts(title_opts=opts.TitleOpts(title='Funnel-基本示例',subtitle='我是副标题'))
    #.set_series_opts(label_opts=opts.LabelOpts(formatter='{b}:{d}%'))#数据展现的格式
      )


#总渲染
pie.render('漏斗图.html')
```

![8](图\8.gif)

将其标签放在外面，并且倒立

```python
from pyecharts.charts import Funnel
from pyecharts import options as opts

cata = ['访问','收藏','加入购物车','提交单位支付','付款成功']
data1 = [30398,15230,10045,8109,5700]

pie = (
    Funnel()
    .add('用户数',[list(z) for z in zip(cata,data1)],
         sort_='descending',#将参数ascending变成descending
         label_opts=opts.LabelOpts(position='outside')#将参数nside变成outside
         )
    .set_global_opts(title_opts=opts.TitleOpts(title='Funnel-基本示例',subtitle='我是副标题'))
    #.set_series_opts(label_opts=opts.LabelOpts(formatter='{b}:{d}%'))#数据展现的格式
      )


#总渲染
pie.render('漏斗倒立图.html')
```

![9](图\9.gif)

# 散点图

```python
from pyecharts.charts import EffectScatter
from pyecharts import options as opts

cata = ['衬衫','毛衣','裤子','风衣','高登鞋','袜子']
data1 = [114,55,27,101,125,27,105]
data2 = [57,134,137,129,145,60,49]

pie = (
    EffectScatter()
    .add_xaxis(cata)
    .add_yaxis('商店A',data1,symbol_size=20)
    .add_yaxis('商店B',data2,symbol_size=30)
    .set_global_opts(title_opts=opts.TitleOpts(title='EffectScatter-基本示例',subtitle='我是副标题'),
                     toolbox_opts=opts.ToolboxOpts()
                     )
    #.set_series_opts(label_opts=opts.LabelOpts(formatter='{b}:{d}%'))#数据展现的格式
      )


#总渲染
pie.render('散点图.html')
```

![10](图\10.gif)

对散点设置颜色渐变

```python
from pyecharts.charts import EffectScatter
from pyecharts import options as opts

cata = ['衬衫','毛衣','裤子','风衣','高登鞋','袜子']
data1 = [114,55,27,101,125,27,105]
data2 = [57,134,137,129,145,60,49]

pie = (
    EffectScatter()
    .add_xaxis(cata)
    .add_yaxis('商店A',data1,symbol_size=20)
    .add_yaxis('商店B',data2,symbol_size=30)
    .set_global_opts(title_opts=opts.TitleOpts(title='EffectScatter-基本示例',subtitle='我是副标题'),
                     toolbox_opts=opts.ToolboxOpts(),
                     visualmap_opts=opts.VisualMapOpts(is_show=True)#设置散点的颜色渐变
                     )
    #.set_series_opts(label_opts=opts.LabelOpts(formatter='{b}:{d}%'))#数据展现的格式
      )


#总渲染
pie.render('散点图渐变.html')
```

![11](图\11.gif)

# 结束语

总的来说，pyecharts在更新过之后，可视化的步骤和之前的相比相差并不大，无非也就是**创建一个实例对象，然后往里面添加数据以及各种配置**

```
步骤		描述							代码示例
1		实例一个具体类型图表的对象			bar = Bar()
2		添加x轴、y轴的具体数据			bar.add_x/yaxis()
3		添加标题等其他配置				bar.set_global_opts()
4		在jupyter notebook中生成图片		render_notebook()
```

